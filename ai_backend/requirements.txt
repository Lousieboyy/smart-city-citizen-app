# FastAPI & Web Server
fastapi
uvicorn
python-multipart

# Database
sqlalchemy
psycopg2-binary

# Durable image storage. Uploads otherwise land in /tmp, which is wiped on every
# redeploy and cold start, so citizen photos did not survive their own report.
vercel_blob

# Security & Auth
python-dotenv
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1

# AI & Image Processing (Running in Lightweight TFLite mode)
ai-edge-litert
opencv-python-headless
pillow

# Pin NumPy 1.x to prevent Segmentation Faults (Exit 139) with TensorFlow/NudeNet
numpy<2.0.0
